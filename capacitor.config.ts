import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clientmanager.dashboard',
  appName: 'Gestor de clientes',
  webDir: 'dist'
};

export default config;
